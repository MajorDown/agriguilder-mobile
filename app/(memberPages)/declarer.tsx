import AppPage from '@/components/AppPage';
import { Text } from 'react-native';

const Declarer = () => {
    return (<AppPage memberOnly title="Déclarer une nouvelle intervention" >
        <Text>page de déclaration des interventions</Text>
    </AppPage>)
}

export default Declarer;