import AppPage from '@/components/AppPage';
import { Text } from 'react-native';

const Options = () => {
    return (<AppPage adminOnly title="Options et paramètres">
        <Text>page pour les options</Text>
    </AppPage>)
}

export default Options;